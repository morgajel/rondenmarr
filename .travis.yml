# Install the apt prerequisites
addons:
  apt:
    packages:
      - https://github.com/gohugoio/hugo/releases/download/v0.45/hugo_0.45_Linux-64bit.deb

# Clean and don't fail
install:
  - rm -rf public || exit 0

# Build the website
script:
  - hugo --theme=octopress

